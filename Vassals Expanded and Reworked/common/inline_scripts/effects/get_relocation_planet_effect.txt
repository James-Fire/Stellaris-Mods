if = {
	limit = { check_variable = { which = total_relocation_planets value >= $NUM$ } }
	random_owned_planet = {
		limit = { has_carrier_flag = relocation_planet }
		remove_carrier_flag = relocation_planet
		save_event_target_as = relocation_planet_$NUM$
		log = "PLANET: \\[this.GetName]"
	}
}